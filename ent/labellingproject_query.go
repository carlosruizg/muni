// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/carlosruizg/muni/ent/labellingproject"
	"github.com/carlosruizg/muni/ent/predicate"
)

// LabellingProjectQuery is the builder for querying LabellingProject entities.
type LabellingProjectQuery struct {
	config
	ctx        *QueryContext
	order      []labellingproject.OrderOption
	inters     []Interceptor
	predicates []predicate.LabellingProject
	modifiers  []func(*sql.Selector)
	loadTotal  []func(context.Context, []*LabellingProject) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LabellingProjectQuery builder.
func (lpq *LabellingProjectQuery) Where(ps ...predicate.LabellingProject) *LabellingProjectQuery {
	lpq.predicates = append(lpq.predicates, ps...)
	return lpq
}

// Limit the number of records to be returned by this query.
func (lpq *LabellingProjectQuery) Limit(limit int) *LabellingProjectQuery {
	lpq.ctx.Limit = &limit
	return lpq
}

// Offset to start from.
func (lpq *LabellingProjectQuery) Offset(offset int) *LabellingProjectQuery {
	lpq.ctx.Offset = &offset
	return lpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lpq *LabellingProjectQuery) Unique(unique bool) *LabellingProjectQuery {
	lpq.ctx.Unique = &unique
	return lpq
}

// Order specifies how the records should be ordered.
func (lpq *LabellingProjectQuery) Order(o ...labellingproject.OrderOption) *LabellingProjectQuery {
	lpq.order = append(lpq.order, o...)
	return lpq
}

// First returns the first LabellingProject entity from the query.
// Returns a *NotFoundError when no LabellingProject was found.
func (lpq *LabellingProjectQuery) First(ctx context.Context) (*LabellingProject, error) {
	nodes, err := lpq.Limit(1).All(setContextOp(ctx, lpq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{labellingproject.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lpq *LabellingProjectQuery) FirstX(ctx context.Context) *LabellingProject {
	node, err := lpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LabellingProject ID from the query.
// Returns a *NotFoundError when no LabellingProject ID was found.
func (lpq *LabellingProjectQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = lpq.Limit(1).IDs(setContextOp(ctx, lpq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{labellingproject.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lpq *LabellingProjectQuery) FirstIDX(ctx context.Context) int {
	id, err := lpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LabellingProject entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LabellingProject entity is found.
// Returns a *NotFoundError when no LabellingProject entities are found.
func (lpq *LabellingProjectQuery) Only(ctx context.Context) (*LabellingProject, error) {
	nodes, err := lpq.Limit(2).All(setContextOp(ctx, lpq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{labellingproject.Label}
	default:
		return nil, &NotSingularError{labellingproject.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lpq *LabellingProjectQuery) OnlyX(ctx context.Context) *LabellingProject {
	node, err := lpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LabellingProject ID in the query.
// Returns a *NotSingularError when more than one LabellingProject ID is found.
// Returns a *NotFoundError when no entities are found.
func (lpq *LabellingProjectQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = lpq.Limit(2).IDs(setContextOp(ctx, lpq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{labellingproject.Label}
	default:
		err = &NotSingularError{labellingproject.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lpq *LabellingProjectQuery) OnlyIDX(ctx context.Context) int {
	id, err := lpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LabellingProjects.
func (lpq *LabellingProjectQuery) All(ctx context.Context) ([]*LabellingProject, error) {
	ctx = setContextOp(ctx, lpq.ctx, "All")
	if err := lpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LabellingProject, *LabellingProjectQuery]()
	return withInterceptors[[]*LabellingProject](ctx, lpq, qr, lpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (lpq *LabellingProjectQuery) AllX(ctx context.Context) []*LabellingProject {
	nodes, err := lpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LabellingProject IDs.
func (lpq *LabellingProjectQuery) IDs(ctx context.Context) (ids []int, err error) {
	if lpq.ctx.Unique == nil && lpq.path != nil {
		lpq.Unique(true)
	}
	ctx = setContextOp(ctx, lpq.ctx, "IDs")
	if err = lpq.Select(labellingproject.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lpq *LabellingProjectQuery) IDsX(ctx context.Context) []int {
	ids, err := lpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lpq *LabellingProjectQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, lpq.ctx, "Count")
	if err := lpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, lpq, querierCount[*LabellingProjectQuery](), lpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (lpq *LabellingProjectQuery) CountX(ctx context.Context) int {
	count, err := lpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lpq *LabellingProjectQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, lpq.ctx, "Exist")
	switch _, err := lpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (lpq *LabellingProjectQuery) ExistX(ctx context.Context) bool {
	exist, err := lpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LabellingProjectQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lpq *LabellingProjectQuery) Clone() *LabellingProjectQuery {
	if lpq == nil {
		return nil
	}
	return &LabellingProjectQuery{
		config:     lpq.config,
		ctx:        lpq.ctx.Clone(),
		order:      append([]labellingproject.OrderOption{}, lpq.order...),
		inters:     append([]Interceptor{}, lpq.inters...),
		predicates: append([]predicate.LabellingProject{}, lpq.predicates...),
		// clone intermediate query.
		sql:  lpq.sql.Clone(),
		path: lpq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LabellingProject.Query().
//		GroupBy(labellingproject.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (lpq *LabellingProjectQuery) GroupBy(field string, fields ...string) *LabellingProjectGroupBy {
	lpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LabellingProjectGroupBy{build: lpq}
	grbuild.flds = &lpq.ctx.Fields
	grbuild.label = labellingproject.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.LabellingProject.Query().
//		Select(labellingproject.FieldName).
//		Scan(ctx, &v)
func (lpq *LabellingProjectQuery) Select(fields ...string) *LabellingProjectSelect {
	lpq.ctx.Fields = append(lpq.ctx.Fields, fields...)
	sbuild := &LabellingProjectSelect{LabellingProjectQuery: lpq}
	sbuild.label = labellingproject.Label
	sbuild.flds, sbuild.scan = &lpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LabellingProjectSelect configured with the given aggregations.
func (lpq *LabellingProjectQuery) Aggregate(fns ...AggregateFunc) *LabellingProjectSelect {
	return lpq.Select().Aggregate(fns...)
}

func (lpq *LabellingProjectQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range lpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, lpq); err != nil {
				return err
			}
		}
	}
	for _, f := range lpq.ctx.Fields {
		if !labellingproject.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lpq.path != nil {
		prev, err := lpq.path(ctx)
		if err != nil {
			return err
		}
		lpq.sql = prev
	}
	return nil
}

func (lpq *LabellingProjectQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LabellingProject, error) {
	var (
		nodes = []*LabellingProject{}
		_spec = lpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LabellingProject).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LabellingProject{config: lpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(lpq.modifiers) > 0 {
		_spec.Modifiers = lpq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	for i := range lpq.loadTotal {
		if err := lpq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (lpq *LabellingProjectQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lpq.querySpec()
	if len(lpq.modifiers) > 0 {
		_spec.Modifiers = lpq.modifiers
	}
	_spec.Node.Columns = lpq.ctx.Fields
	if len(lpq.ctx.Fields) > 0 {
		_spec.Unique = lpq.ctx.Unique != nil && *lpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, lpq.driver, _spec)
}

func (lpq *LabellingProjectQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(labellingproject.Table, labellingproject.Columns, sqlgraph.NewFieldSpec(labellingproject.FieldID, field.TypeInt))
	_spec.From = lpq.sql
	if unique := lpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if lpq.path != nil {
		_spec.Unique = true
	}
	if fields := lpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, labellingproject.FieldID)
		for i := range fields {
			if fields[i] != labellingproject.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := lpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lpq *LabellingProjectQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lpq.driver.Dialect())
	t1 := builder.Table(labellingproject.Table)
	columns := lpq.ctx.Fields
	if len(columns) == 0 {
		columns = labellingproject.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lpq.sql != nil {
		selector = lpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lpq.ctx.Unique != nil && *lpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range lpq.predicates {
		p(selector)
	}
	for _, p := range lpq.order {
		p(selector)
	}
	if offset := lpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LabellingProjectGroupBy is the group-by builder for LabellingProject entities.
type LabellingProjectGroupBy struct {
	selector
	build *LabellingProjectQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lpgb *LabellingProjectGroupBy) Aggregate(fns ...AggregateFunc) *LabellingProjectGroupBy {
	lpgb.fns = append(lpgb.fns, fns...)
	return lpgb
}

// Scan applies the selector query and scans the result into the given value.
func (lpgb *LabellingProjectGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lpgb.build.ctx, "GroupBy")
	if err := lpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LabellingProjectQuery, *LabellingProjectGroupBy](ctx, lpgb.build, lpgb, lpgb.build.inters, v)
}

func (lpgb *LabellingProjectGroupBy) sqlScan(ctx context.Context, root *LabellingProjectQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lpgb.fns))
	for _, fn := range lpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lpgb.flds)+len(lpgb.fns))
		for _, f := range *lpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LabellingProjectSelect is the builder for selecting fields of LabellingProject entities.
type LabellingProjectSelect struct {
	*LabellingProjectQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lps *LabellingProjectSelect) Aggregate(fns ...AggregateFunc) *LabellingProjectSelect {
	lps.fns = append(lps.fns, fns...)
	return lps
}

// Scan applies the selector query and scans the result into the given value.
func (lps *LabellingProjectSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lps.ctx, "Select")
	if err := lps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LabellingProjectQuery, *LabellingProjectSelect](ctx, lps.LabellingProjectQuery, lps, lps.inters, v)
}

func (lps *LabellingProjectSelect) sqlScan(ctx context.Context, root *LabellingProjectQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lps.fns))
	for _, fn := range lps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
